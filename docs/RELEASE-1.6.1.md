# PicLite 图轻 v1.6.1

- 文件夹监控恢复为主窗口独立页面，可在页面内切换、启停、移除和保存多个监控任务。
- 批量重命名保持为独立内置插件页面，不再混入应用设置；设置页的控件布局重新对齐。
- 监控任务新增“处理完成后弹出悬浮窗”开关，关闭时仅保留系统通知和活动记录。
- 监控压缩悬浮窗固定尺寸并限制在屏幕可操作区域内，图片尺寸变化不会把窗口推到屏幕边缘之外。
- 监控任务支持复用父文件夹提取命名规则，默认规则可将 `【1-1】` 补齐为 `0101`。
- 保留 JFIF 支持和 Windows 便携版发布能力。

# PicLite 图轻 v1.7.0

- 批量重命名插件支持从文件名或父目录内容提取词语、英文首字母，并可自定义词语连接符。
- 文件夹监控任务支持自定义名称；每个任务可独立设置完成通知和处理后悬浮窗。
- 等比例缩放支持直接输入目标宽高，默认读取原图尺寸，修改任一边会按原图比例自动适配另一边。
- 水印新增图片水印和盲水印，图片水印支持单点定位、平铺、透明度、尺寸和旋转。
- 修复图片水印导出为 WebP 时失效的问题。
- 优化导入、实时试压和带水印压缩性能；桌面端改用二进制 Base64 IPC、原图缓存和原生图片水印编码，并只保留最新预览任务。

# PicLite 图轻 v1.7.1

- 修复动态 WebP 压缩后只剩静态首帧的问题。
- 工作台、文件夹监控和快捷压缩现在会保留动态 WebP 的全部帧、帧时长、循环次数与透明背景，并支持等比例缩放。
- 动态 WebP 与 GIF 支持文字水印、图片水印和盲水印；GIF 转 WebP 时也可保留动画并添加水印。
- 静态图智能压缩改为单次真实编码，去掉导入和调参时重复执行的 6～27 次候选编码；目标体积模式仍会按需多次逼近。
- 当动态 WebP 选择 JPG 或 PNG 等静态格式时，会停止并显示原因，避免静默丢失动画。
